const { expect } = require('chai');

let msg = '';

const isElementPresent = async (locatorKey) => {
 console.log('function to check the presence of an element');
}


module.exports = {
  isElementPresent
}
