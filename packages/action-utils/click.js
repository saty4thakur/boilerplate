const { expect } = require('chai');
const swipe = require('./swipe');

const clickOnElement = async (locatorKey) => {
  console.log('function to click on an element');
}

module.exports = {
  clickOnElement
};
